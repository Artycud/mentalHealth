/**
 * Thai labels for the admin panel (BRIEF §11).
 *
 * PHASE 5 fills this. The admin panel is one account for the student council,
 * used to pull statistics for the school report. Thai labels throughout, same
 * palette and fonts as the student side but a denser layout.
 */

export const admin = {
  // TODO(phase 5): summary strip, theme control, charts, sessions list,
  // filters, CSV export, delete.
} as const;
